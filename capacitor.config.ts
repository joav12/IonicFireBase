import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'modelo_firebaseapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
